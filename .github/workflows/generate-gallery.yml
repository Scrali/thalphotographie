name: Generate gallery.auto.json

on:
  push:
    paths:
      - 'photos/**'
      
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        
      - name: Generate gallery.auto.json
        run: |
          echo "{" > photos/gallery.json
          first=true
          for dir in photos/*/ ; do
            folder=$(basename "$dir")
            if [ "$folder" != "gallery.auto.json" ]; then
              files=$(ls "$dir" | grep -E '\.(jpg|jpeg|png|webp)$' | sed 's/.*/"&"/' | paste -sd, -)
              if [ ! -z "$files" ]; then
                if [ "$first" = true ]; then
                  first=false
                else
                  echo "," >> photos/gallery.auto.json
                fi
                echo "  \"$folder\": [$files]" >> photos/gallery.auto.json
              fi
            fi
          done
          echo "}" >> photos/gallery.auto.json

      - name: Commit updated gallery.auto.json
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add photos/gallery.auto.json
          git commit -m "Auto-update gallery.auto.json" || echo "No changes"
          git push
